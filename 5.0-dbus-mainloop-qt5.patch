--- origsrc/PyQt-gpl-5.0/configure.py	2013-06-16 08:30:57.000000000 -0500
+++ src/PyQt-gpl-5.0/configure.py	2013-08-16 15:23:45.741005700 -0500
@@ -59,7 +59,7 @@ class ModuleMetadata:
 
 # The module meta-data.
 MODULE_METADATA = {
-    'dbus':             ModuleMetadata(qmake_QT=['-gui'], qmake_TARGET='qt',
+    'dbus':             ModuleMetadata(qmake_QT=['-gui'], qmake_TARGET='qt5',
                                 in_consolidated=False),
     'QAxContainer':     ModuleMetadata(qmake_QT=['axcontainer']),
     'Qt':               ModuleMetadata(qmake_QT=['-core', '-gui'],
--- origsrc/PyQt-gpl-5.0/dbus/dbus.cpp	2013-06-16 08:31:14.000000000 -0500
+++ src/PyQt-gpl-5.0/dbus/dbus.cpp	2013-08-16 15:22:58.376296600 -0500
@@ -401,29 +401,29 @@ static PyMethodDef module_functions[] =
 
 // The module entry point.
 #if PY_MAJOR_VERSION >= 3
-PyMODINIT_FUNC PyInit_qt()
+PyMODINIT_FUNC PyInit_qt5()
 {
     static PyModuleDef module_def = {
         PyModuleDef_HEAD_INIT,
-        "qt",
+        "qt5",
         NULL,
         -1,
         module_functions,
     };
 
     // Import the generic part of the Python DBus bindings.
-    if (import_dbus_bindings("dbus.mainloop.qt") < 0)
+    if (import_dbus_bindings("dbus.mainloop.qt5") < 0)
         return 0;
 
     return PyModule_Create(&module_def);
 }
 #else
-PyMODINIT_FUNC initqt()
+PyMODINIT_FUNC initqt5()
 {
     // Import the generic part of the Python DBus bindings.
-    if (import_dbus_bindings("dbus.mainloop.qt") < 0)
+    if (import_dbus_bindings("dbus.mainloop.qt5") < 0)
         return;
 
-    Py_InitModule("qt", module_functions);
+    Py_InitModule("qt5", module_functions);
 }
 #endif
--- origsrc/PyQt-gpl-5.0/dbus/dbus.sbf	2013-06-16 08:31:14.000000000 -0500
+++ src/PyQt-gpl-5.0/dbus/dbus.sbf	2013-08-16 15:22:34.979958400 -0500
@@ -1,5 +1,5 @@
 # This is the build file for the dbus support module for Qt v4.
 
-target = qt
+target = qt5
 sources = dbus.cpp
 moc_headers = helper.h
